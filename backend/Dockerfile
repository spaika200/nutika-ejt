FROM node:20-alpine AS builder

WORKDIR /app

COPY package*.json ./
COPY tsconfig.json ./
# Assuming prisma schema is in prisma/
# COPY prisma ./prisma/

RUN npm ci

COPY src ./src
RUN npm run build

FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY --from=builder /app/dist ./dist
# COPY --from=builder /app/prisma ./prisma

EXPOSE 5000

CMD ["npm", "start"]
